{
  "name": "stereolove",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A head-coupled op-art installation about perception, perspective, and negotiated reality.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write .",
    "check": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "vite": "^8.0.13",
    "vitest": "^4.1.6"
  }
}
